<?php

return [
    /*
     * Мерить ли число подписчиков канала.
     *
     * Выключатель нужен по двум причинам. Первая: замер ходит в телеграм через
     * бота, и на стенде без живого токена задача просто копилась бы в опросе.
     * Вторая: в тестах опрос проверяют по составу задач, и ежедневный замер —
     * лишний элемент в каждом таком наборе; он выключен в phpunit.xml и
     * включается точечно там, где его и проверяют.
     */
    'enabled' => env('BROADCAST_MEASURE_SUBSCRIBERS', true),
];
