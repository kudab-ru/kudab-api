<?php

declare(strict_types=1);

namespace App\Exceptions;

use RuntimeException;

/**
 * Состав подборки после замены не собрался — откатить всё.
 *
 * Исключение, а не возврат значения, по одной причине: замена идёт в
 * транзакции, а Laravel коммитит её при любом НОРМАЛЬНОМ выходе из замыкания.
 * Откат случается только по Throwable. Возврати мы null — в базе осталась бы
 * подменённая связь, подпись про прежнюю тройку и снятый чужой пост, причём
 * под ответом «подборка осталась как была».
 */
final class DigestRecomposeFailed extends RuntimeException {}
